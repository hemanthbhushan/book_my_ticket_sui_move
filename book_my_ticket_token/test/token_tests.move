#[test_only]
module TicketProject::ticket_token_tests{
    use std::option;
    use TicketProject::ticket_token::{Self, TICKET_TOKEN};
    use sui::pay;
    use sui::url;
    use sui::test_scenario;
    use sui::transfer;
    use sui::tx_context;
    use std::string;
    use std::ascii;




}